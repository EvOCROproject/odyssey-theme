---
const ALT = 'EvOCRO';
const SRC = '/odyssey-theme/assets/images/logo.png';
const size = Astro.props.size || 'large';
---

<a href="/" aria-label={ALT} class={`site-logo ${size}`}>
  <img src={SRC} alt={ALT} />
</a>

<style>
  .site-logo {
    display: inline-flex;
    align-items: center;
    text-decoration: none;
  }

  /* Logo grande (header) */
  .site-logo.large img {
    height: 96px;
    width: auto;
    display: block;
  }

  /* Logo piccolo (footer) */
  .site-.small img {
    height: 48px;
    width: auto;
    display: block;
  }

  @media (max-width: 640px) {
    .site-.large img { height: 72px; }
    .site-.small img { height: 40px; }
  }
</style>
