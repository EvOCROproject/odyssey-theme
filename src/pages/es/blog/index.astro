---
import Layout from '../../../layouts/Page.astro';
import BASE from '../../../utils/base-url';
---

<Layout
  title="Blog | EvOCRO"
  description="Entradas del blog sobre el proyecto EvOCRO."
>
  <div class="lang-switcher">
    <a href={`${BASE}blog/`}>EN</a>
    <a href={`${BASE}it/blog/`}>IT</a>
    <a href={`${BASE}es/blog/`} class="active">ES</a>
  </div>

  <h1>Blog</h1>
  <p>Artículos y actualizaciones en español del proyecto EvOCRO.</p>
</Layout>

<style>
  .lang-switcher {
    display: flex;
    justify-content: flex-end;
    gap: .5rem;
    margin-bottom: 1rem;
  }
  .lang-switcher a {
    padding: .25rem .75rem;
    text-decoration: none;
    border-radius: 999px;
    border: 1px solid var(--theme-on-surface-2);
    font-size: var(--font-size-sm);
  }
  .lang-switcher a.active {
    background: var(--theme-primary);
    color: var(--theme-on-primary);
  }
</style>
